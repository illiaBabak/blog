class PageConfig {
  start = '/start';
  login = '/login';
  redirect = '/redirect';
  main = '/main';
  confirm = '/confirm';
  profile = '/profile';
  comments = '/comments';
}

export const pageConfig = new PageConfig();
